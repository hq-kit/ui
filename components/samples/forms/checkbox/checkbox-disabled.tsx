import { Checkbox } from "@/components/ui/checkbox"

export default function CheckboxDisabled() {
  return (
    <Checkbox isDisabled name="toggle-checkbox-disabled">
      Enable notifications
    </Checkbox>
  )
}
