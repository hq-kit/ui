---
title: Bar Chart
description: A bar chart provides a way of sDemoing data values represented as component bars. It is sometimes used to sDemo trend data, and the component of multiple data sets side by side.
order: 1
published: true
references: ['https://recharts.org/en-US/api/BarChart']
---

## Installation
<Install component='chart' />
## Basic
Charts are used to display data in a clear and concise manner. They are ideal for presenting information in a visually appealing and easy-to-understand format.
<Demo center className='w-full' component='charts/bar/bar-chart-basic-demo' />

## Horizontal
Use horizontal to display multiple data sets on the same chart.
<Demo center className='w-full' component='charts/bar/bar-chart-horizontal-demo' />


## Label
Use label to display multiple data sets on the same chart.
<Demo center className='w-full' component='charts/bar/bar-chart-label-demo' />

## Inset Label
Use horizontal to display multiple data sets on the same chart.
<Demo center className='w-full' component='charts/bar/bar-chart-horizontal-label-inset-demo' />

## Custom Label
Use custom label to display multiple data sets on the same chart.
<Demo center className='w-full' component='charts/bar/bar-chart-custom-label-demo' />

## Mixed
Use mixed to display multiple data sets on the same chart.
<Demo center className='w-full' component='charts/bar/bar-chart-mixed-demo' />

## Negative
Use negative to display multiple data sets on the same chart.
<Demo center className='w-full' component='charts/bar/bar-chart-negative-demo' />

## Multiple
Use multiple to display multiple data sets on the same chart.
<Demo center className='w-full' component='charts/bar/bar-chart-multiple-demo' />

## Stacked Legend
Use stackedLegend to display multiple data sets on the same chart.
<Demo center className='w-full' component='charts/bar/bar-chart-stacked-legend-demo' />

## Active
Use active to display multiple data sets on the same chart.
<Demo center className='w-full' component='charts/bar/bar-chart-active-demo' />

## Controlled
Sometimes you might want to control the chart data dynamically.
<Demo center className='w-full' component='charts/bar/bar-chart-controlled-demo' />
